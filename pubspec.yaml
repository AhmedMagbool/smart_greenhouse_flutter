name: smart_greenhouse_flutter_dashboard
description: A smart greenhouse dashboard in Flutter.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.65.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
